# CRM 业务数据分析与结算平台交接

最后更新：2026-08-21（Asia/Shanghai）

## 使用方式

1. 新会话先完整阅读 `AGENTS.md`，确认长期业务规则和安全边界。
2. 再阅读本文件，了解当前开发与部署状态。
3. 执行 `git status --short`、`git log -1 --oneline` 核对现场。本文件是交接快照，不替代 Git、代码、运行日志或财务最新确认口径。
4. 完成重要功能、部署排障或准备暂停工作时，直接更新本快照，并删除已经失效的临时信息。

严禁在本文件中写入密码、令牌、Cookie、私钥、工作簿密码、真实客户信息或完整业务数据。

## 当前仓库状态

- 分支：`main`
- 当前提交：`6f4c7f7`
- 提交说明：`feat: 更新图表模板和数据迁移逻辑，支持双横轴分组柱状展示，优化测试用例`
- 当前未提交改动：年度报表跟随单选年份筛选；新增年度拆装异常对账清单；新增“2026 年度拆机汇总”，仅按业务属性为拆机且 CRM 源表完工日期在 2026 年的记录纳入，按月汇总业务数、线数、月平均计量和优惠资费；销售业务净增固定为当前计量规则“新增量”，净增仅扣减同属新增量、业务属性为拆机且 CRM 完工日期在 2026 年的记录，2026 年度拆机独立展示而不混入净增；销售分析新增“2026 年度新增复核”，以初始完工日期在 2026 年的全部线路为新增总体，独立于全局筛选，并分层展示活跃、当年拆机及其他待复核状态；总览新增“2026 年新增当年拆机确认”卡片及逐条清单，新增取初始完工日期、拆机取 CRM 完工日期，两者均在 2026 年且业务属性为拆机才纳入，不受全局筛选影响。年度筛选改为按业务源日期取数：拆机以 CRM 完工日期筛选，其他业务以初始完工日期筛选，缺失时才用完工日期兜底；总览、销售和业务分析在单选年份时均传入该年度口径。业务属性不再由计量规则反向补成新装，动态计量规则严格按初始完工日期、固定新增量年度 2026、月差小于13为新量及大于120为超期计算；业务进展、总览、双线和图表的新增统一取当前“新增量”，全年业务拆装情况仍按业务属性统计。统一数据中心与结算中心的结算待复核判断；服务商政策分布在同一筛选结果上复用一次聚合结果；总览移除新装卡片，供应商/服务商顶部图改为线路数与月平均计量双轴分布图；服务商分布改为横向复合图（线路数柱状、月平均计量独立金额轴折线），历史系统模板自动迁移；月度计量趋势改为两条费用折线（计量、资费）加线路数柱状图，并调整为业务分析页独立通栏、高度 430px 的图表；金额轴在数据集中且波动范围较窄时启用带起点标注的截断轴，保留完整悬浮值；图内数值标签去除单位；所有自定义图表改为全量分类加图内滚动，取消“其他”合并；双线折算改为仅使用 CRM 源表“月平均计量”，不以月平均资费、优惠资费或年优惠资费替代，并增加折算记录数以便对账；总览/结算明细仍展示 CRM 源表月平均资费。此前的数据版本字段映射质量识别、历史版本安全删除、字段命名统一、CRM 结算准备明细等改动仍未提交。
- 远端仓库：`hydintern3/crm-settlement`

## 已落地能力

- 多版本重复设备改为字段级合并：较新非空字段覆盖，较新空值保留已有值，避免稀疏不活跃表擦除完整活跃表字段。
- 新增独立供应商分析；供应商严格来自供应商字段，服务商分析严格按 I/II 服务编号及简称汇总。
- 月平均计量统一按万元保留6位小数；所有图表始终显示数值标签，历史模板自动迁移。
- CSV 导出已加入设备编号、月平均资费、优惠资费、营销增值费用、授权价判断、业务毛利等全部已建模非敏感核心字段，并同时输出月平均计量原值和万元值。
- 有聚合意义的业务、销售、供应商和服务商分析表已增加总计/总览，比率和均值按总体重新计算。
- 图表默认分类数由 Top 10 放宽到 Top 50；旧版仍使用“Top 10 + 合并其他”的模板会在读取时迁移到 Top 50，减少“其他”聚合，其他自定义数量保持不变。
- 部分字段缺失的 CRM 记录仍完整进入版本管理、查询、导出和已有字段筛选；原“待人工复核”已明确为“结算待复核”，并拆分显示缺计量规则、缺月平均计量、年付/两年付原因。表头空格与换行变体可归一化识别，业务表识别不再依赖少数固定字段全部存在。
- 上传链路允许单次最多 100 MB 原始表格，框架和 Nginx 使用 110 MB 外层限制；413 或非 JSON 响应会显示可理解的中文错误，不再暴露 `Unexpected token`。
- 筛选选项采用自适应宽度和完整换行显示；供应商、负责人、业务名称、服务编号等长文本不再以省略号隐藏。
- 统一查询、CSV 导出、图表维度和质量报表已统一字段命名：业务属性等 CRM 原始字段明确标注“（CRM）”，平台推导字段明确标注“（平台）”；计量规则对外仅显示“计量规则（按当前日期计算）”，CRM 原始公式结果保留在内部用于审计和重算。
- 总览金额显示统一改为元并使用千分位；总记录、实际活跃和拆机卡片增加月平均计量/资费摘要，移除独立月平均计量卡片；新增业务类别、计算方式筛选，月度趋势支持计量、资费和线路数双轴展示，供应商/服务商图表改为分布口径。
- 总览新增净增长分析及双线拆装比。双线折算只按 CRM 源表月平均计量：低于10,000元不折算，10,000至11,999元计1线，此后每满2,000元增加1线、最高20线；新增和拆机同口径。双线表分列展示原始线路、符合折算记录、计量折算线路和考核合计，后者为原始线路加计量折算线路。考核目标支持68%、75%及自定义，待补线路按拆机合计线路/目标比例减新增合计线路计算，最小为0。
- 管理员登录、服务端数据上传和数据版本管理。
- 可选择多个 CRM 全量数据版本，按设备编号去重并整合；空设备编号保留并进入质量检查。
- 已扩展联系人固话、计算状态、分期计算标识、拆机类型和用户拆机原因等字段支持，并增加更多筛选维度。
- 总览页支持自定义图表模板：柱状图、折线图、面积图、饼图、环形图、堆叠柱状图和散点图。
- 图表支持维度、系列、指标、聚合、排序、Top N 和尺寸配置；支持创建、编辑、复制、固定、取消固定、调整顺序、归档和恢复。
- 固定图表使用当前版本整合后的 `filteredRows`，与全局筛选、动态规则和数据版本联动；可查看聚合后的图表数据。
- 原有四张总览图已迁移为默认模板。
- 已读取财务提供的 `AI开发结果20260804去密码.xlsx`，并将当前 CRM 能可靠支持的字段落地到结算中心：明细表展示业务、服务、状态、付款周期、当前计量规则、月平均资费/计量和 CRM 业务毛利；候选汇总按 I 服务编号统计记录、活跃、可计算、结算待复核、月平均计量和 CRM 业务毛利。
- 结算中心候选汇总使用当前筛选后的 `filteredRows`，年付/两年付复核依据付款周期和分期计算标识，候选排序按原始数值而非格式化金额字符串。
- 服务商分析新增 I/II 服务组合分析：按同一 CRM 记录的 I 服务和 II 服务组合汇总，单侧服务记录仍保留；新量、存量使用“计量规则（按当前日期计算）”。新增“服务商新量、存量政策分布”，双侧服务记录会分别计入 I 服务和 II 服务，合计是服务项口径而非去重业务记录口径。
- 全局扩展筛选新增“是否低于授权价”，联动指标、图表、统一查询和服务商政策分布。源表没有该字段时不会生成筛选项；政策分布中缺失值显示“未采集”。
- 财务确认“全年业务拆装情况”须按源表完工日期而非平台统计完工日期统计。已调整 `buildMonthlyBusiness`：仅汇总当前年度的源表完工日期，按月统计业务属性为新装/拆机的线数和月平均计量；完工日期缺失记录不进入该年度月表。完工批次留存分析和其他平台日期兜底逻辑不受影响。
- 业务分析年度报表在筛选器单选年份时跟随该年份；未单选时取当前筛选记录中源表完工日期的最新年份。年度拆装异常对账表列出完工日期为空、初始/完工日期不一致、拆机年度归属异常、年度内业务属性待确认、设备编号重复和月平均计量为空的记录，仅供人工复核。
- 数据中心和结算中心统一调用 `isSettlementReviewCandidate`，避免年付/两年付、缺计量规则或缺月平均计量的待复核口径分叉。
- 总览金额继续按元显示并使用千分位；业务总记录、实际活跃和拆机卡片显示月平均计量与月平均资费，新装卡片已移除。供应商分布、服务商分布图均显示线路数和月平均计量，采用双纵轴和折线叠加，避免多指标柱状图拥挤；业务类别和计算方式筛选已联动全部页面结果。

## 关键代码入口

- 图表模板定义：`platform/app/lib/chart-template.ts`
- 图表聚合：`platform/app/lib/chart-aggregation.ts`
- 服务端模板存储：`platform/app/lib/server/dashboard-store.ts`
- 图表模板接口：`platform/app/api/dashboard/templates/`
- 图表编辑器：`platform/app/components/ChartBuilder.tsx`
- 图表渲染与管理：`platform/app/components/AnalyticsCharts.tsx`
- 总览数据和筛选入口：`platform/app/page.tsx`
- 容器编排：`compose.yaml`
- Nginx 子路径配置示例：`deploy/nginx/crm-location.conf`
- 云主机部署说明：`docs/cloud-host-deployment.md`
- CI 与镜像发布：`.github/workflows/ci.yml`

## 最近验证结果

在提交 `a187424` 加当前未提交的数据版本映射质量改动上已完成：

- `npm run verify`：通过。
- 自动化测试：19 项通过；新增覆盖不完整业务行仍可发布、旧空字段快照判为不可用、服务端拒绝发布，以及历史版本删除保护。
- 依赖安全检查：无 high 级别漏洞。
- 生产构建：通过，发布检查确认构建产物不包含本地快照或业务表格。
- 上传回归：构造约 1.22 MB 的 multipart 请求后，已越过原 1 MB 框架限制并到达上传接口，返回 `application/json` 业务校验结果。
- 包体预算检查通过：图表按需包 594.9 KB / 600 KB，导入按需包 365.1 KB / 450 KB，首屏包 78.2 KB / 350 KB。Vite 仍有通用的大 chunk 提示，后续增加图表能力时继续关注拆包。
- 本轮新增结算示例表后重新执行 `npm.cmd run verify`：lint、typecheck、生产依赖审计、构建、20 项测试、发布数据检查和包体预算均通过；`git diff --check` 通过。图表按需包 595.3 KB / 600 KB，导入按需包 365.1 KB / 450 KB，首屏包 87.9 KB / 350 KB。
- 本轮服务组合和授权价筛选后重新执行 `npm.cmd run verify`：lint、typecheck、生产依赖审计、构建、21 项测试、发布数据检查和包体预算均通过。图表按需包 595.3 KB / 600 KB，导入按需包 365.1 KB / 450 KB，首屏包 91.8 KB / 350 KB。
- 本轮年度拆装口径调整后重新执行 `npm.cmd run verify`：lint、typecheck、生产依赖审计、构建、22 项测试、发布数据检查和包体预算均通过。图表按需包 595.3 KB / 600 KB，导入按需包 365.1 KB / 450 KB，首屏包 91.9 KB / 350 KB。
- 本轮年度年份联动、异常对账及待复核口径统一后重新执行 `npm.cmd run verify`：lint、typecheck、生产依赖审计、构建、22 项测试、发布数据检查和包体预算均通过。图表按需包 595.3 KB / 600 KB，导入按需包 365.1 KB / 450 KB，首屏包 93.6 KB / 350 KB；`git diff --check` 通过。
- 本轮总览卡片和供应商/服务商图表优化后重新执行 `npm.cmd run verify`：lint、typecheck、生产依赖审计、构建、22 项测试、发布数据检查和包体预算均通过。图表按需包 596.7 KB / 600 KB，导入按需包 365.1 KB / 450 KB，首屏包 93.8 KB / 350 KB。
- 本轮月度计量趋势布局与金额截断轴调整后：`npm.cmd run lint`、`npm.cmd run typecheck`、`git diff --check` 通过；在允许构建临时文件写入后执行 `npm.cmd test`，构建、发布数据检查、22 项测试和包体预算全部通过（图表按需包 597.7 KB / 600 KB）。完整 `npm.cmd run verify` 未完成，原因是当前环境无法访问 npm audit registry。
- 本轮服务商分布改为横向柱状/折线双轴图并提升模板架构至版本 7 后：`npm.cmd run lint`、`npm.cmd run typecheck`、`git diff --check` 通过；`npm.cmd test` 的构建、发布数据检查、22 项测试和包体预算均通过（图表按需包 598.1 KB / 600 KB）。
- 月度计量趋势图已按财务口径调整为：月平均计量、月平均资费使用元轴折线；线路数使用独立线路轴柱状图。调整后 `npm.cmd run verify` 仍通过，22 项测试全部通过。
- 自定义图表的数据标签已统一只显示数值，单位保留在坐标轴和悬浮提示中。图表模板架构升至版本6：所有历史模板自动迁移为全量分类展示，取消“其他”合并；柱状、折线、面积和堆叠图在分类过多时通过图内滚动条浏览全部类别，饼图/环图保留全部扇区并在分类过多时隐藏扇区标签、通过滚动图例和悬浮提示查看。`npm.cmd run verify` 通过，22项测试全部通过。
- 本轮新增“2026 年度拆机汇总”后：`npm.cmd run typecheck`、`node --experimental-strip-types --test tests/data-model.test.mjs`、`npm.cmd test` 和 `git diff --check` 通过。完整测试共 24 项通过；新增回归测试覆盖仅纳入“拆机 + CRM 完工日期属于 2026 年”的记录，排除其他年份、新装和仅有初始完工日期的记录。
- 本轮调整销售业务净增情况后：`npm.cmd run typecheck`、`node --experimental-strip-types --test tests/data-model.test.mjs`、`npm.cmd test` 和 `git diff --check` 通过。完整测试共 25 项通过；新增回归测试覆盖 2026 新增口径、当年新增当年拆机与 2026 年度拆机的独立统计。
- 本轮修正双线折算字段后：`npm.cmd run typecheck`、`node --experimental-strip-types --test tests/data-model.test.mjs`、`npm.cmd test` 和 `git diff --check` 通过。完整测试共 25 项通过；新增回归覆盖“优惠资费”高值不参与折算、月平均资费各档位及 20 线封顶。
- 本轮按财务确认的计量规则口径调整后：`npm.cmd run typecheck`、`node --check scripts/sync-local-folder.mjs`、`npm.cmd test` 和 `git diff --check` 通过。完整测试共 26 项通过；新增回归覆盖固定 2026 年新增量、13/120 月边界、初始完工日期缺失时保留 CRM 结果、变更+新增量纳入新增及新装+存量不纳入新增。
- 本轮新增“2026 年度新增复核”后：`npm.cmd test` 和 `git diff --check` 通过。构建、发布数据检查、27 项自动化测试和包体预算检查均通过；新增回归覆盖初始完工日期在 2026 年的活跃、当年拆机、其他年度拆机、拆机日期缺失、非活跃未拆机、活跃状态缺失及非 2026 记录排除。
- 本轮总览新增“2026 年新增当年拆机确认”后：`npm.cmd run typecheck`、数据模型回归和 `npm.cmd test` 均通过。构建、发布数据检查、28 项自动化测试及包体预算检查均通过；回归覆盖初始完工日期与完工日期均在 2026 年的拆机记录纳入，以及任一日期不在 2026 年或非拆机业务属性的记录排除。
- 财务进一步确认初始完工日期与完工日期不一致是正常的双日期口径表现，年度拆装异常对账不再将其列为异常；完工日期为空仅对拆机记录提示复核。
- 总览净增长分析已改为“2026 年度净增长分析”：新增取当前新增量口径，拆机仅取业务属性为拆机且 CRM 完工日期在 2026 年的记录，并增加年度净增长线路列，避免历史拆机混入年度结果。

以上是当前工作树的实际验证结果。任何后续代码变更后都必须重新运行相应检查。

## 当前部署状态

- 2026-08-19 核查导出文件 `CRM筛选结果_2026-08-19.csv`：原始 `第一批活跃3442去密码.xls / Worksheet` 有3442行，但设备编号唯一值为3441；同一设备编号在Excel第2331行和第2384行重复。两行日期、活跃状态、供应商和月平均计量一致，业务属性分别为新装和变更。平台按已确认的设备编号唯一键去重，导出3441条属于预期去重结果，不是筛选器漏数；后出现的第2384行按当前策略保留。
- 3442行业务表全字段显示 `--` 的根因已复现：`Worksheet` 的65个表头多数带前导空格，已部署旧映射按精确表头匹配而全部落空；当前工作树的空白归一化修复可正确映射设备编号、业务名称、负责人、完工日期、活跃状态和计量规则。需部署新镜像后重新选择 `Worksheet` 上传；旧空字段版本不能自动修复。`Sheet1` 为汇总/透视表，不应选择。
- 2026-08-19 直接以当前代码复核 `第一批活跃3442去密码.xls / Worksheet`：3442条的设备编号、业务属性、业务名称、负责人、供应商、I服务编号、有效日期、活跃状态、计量规则和月平均计量均为3442/3442可映射，业务判定为新装2834条、变更608条。线上出现“359条拆机正常 + 3442条待确认/--”说明整合选择了旧空字段快照；必须重新上传3442条源表后再与359条版本整合。
- 用户已确认当前线上平台部署完成。实际发布约 3,000 条业务数据时遇到框架默认 1 MB multipart 限制；修复代码已通过验证但尚需构建并部署新镜像，同时重新安装 Nginx 片段。
- 云主机网络无法稳定访问 GHCR，当前采用“从 GitHub Actions 下载离线镜像，再通过 SCP 上传云主机”的部署方式。
- 已新增 `deploy/offline-deploy.sh`，用于在 SCP 后自动校验 SHA256、加载 `latest`、仅纠正 `.env` 的 `CRM_IMAGE`、以 `--pull never --force-recreate` 重建容器并等待健康检查；不删除数据卷。尚未在真实服务器执行验证。
- 当前上传修复要求：单次原始表格总大小不超过 100 MB，应用框架与 Nginx 外层限制为 110 MB。部署新镜像后必须把新版 `deploy/nginx/crm-location.conf` 重新安装到 `/etc/nginx/snippets/` 并重载 Nginx。

不得把真实密码或令牌复制到命令历史、交接文件或问题截图中。

## 部署关键约束

- 应用默认监听宿主机 `127.0.0.1:3100`，由 Nginx 对外代理 `/crm/`。
- `/crm/assets/` 必须单独代理到应用的 `/assets/`，否则页面静态资源会失败。
- 数据版本、上传文件、图表模板及审计记录必须落在持久化的 `CRM_DATA_DIR`；升级时保留 Compose 数据卷 `crm-settlement_crm-data`。
- 只有服务器 IP 也可以部署，但生产登录需要 HTTPS。直接使用 HTTP 可能因 Secure Cookie 无法保存而出现登录画面闪烁后返回登录页。
- 离线部署时使用 `--pull never`，并在启动前通过 `docker image inspect <完整镜像标签>` 确认 Compose 引用的标签确实存在。

## 待办与风险

1. 提交并构建当前未提交改动的新镜像；部署后重新上传3442条源表的 `Worksheet`，再与359条拆机原始版本生成新的整合版本。旧上传版本和旧整合版本不会自动重算。
2. 重新安装 Nginx 片段并验证约 3,000 条业务数据可以发布。
3. 在真实服务器执行一次 `sudo bash /opt/crm-settlement/deploy/offline-deploy.sh`，确认旧 SHA 配置自动切换为 `latest`，后续更新无需手工打标签或修改 `.env`。
4. 确定只有 IP 场景下的 HTTPS 方案并验证浏览器信任链；最终方案尚未确认。
5. 图表模板目前采用服务器文件持久化，适合单实例部署；若未来扩展为多实例，需要迁移到共享数据库或对象存储，并处理并发写入。
6. 图表构建器只提供可审计的字段、聚合和展示配置，不支持任意脚本或任意公式；如扩展计算字段，必须设计安全表达式和版本审计。
7. 毛利、目标、正式结算、收付、销账和发票仍受输入数据与正式规则缺失限制，继续遵循 `AGENTS.md` 的人工复核边界。

## 下次交接更新清单

- 更新日期、分支、HEAD 提交和工作树状态。
- 将“当前部署状态”改为现场实际结果，不能把计划写成已完成。
- 记录本轮实际执行的测试、构建和部署验证；未执行项明确注明。
- 更新待办、阻塞项和风险，删除已经失效的信息。
- 新增长期有效的确认事实时，同时提炼到 `AGENTS.md`。
- 检查文档中不存在凭据、敏感业务数据或不必要的服务器标识。
