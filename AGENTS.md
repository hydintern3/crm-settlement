# CRM 业务数据分析与结算优化

## 项目目标

基于 CRM 导出数据，逐步形成可复核、可追溯的业务进展分析和结算工作流。当前优先完成数据口径、规则、异常场景和人工审批边界的梳理，再推进统计分析、结算测算和流程自动化。

## 当前资料与优先级

1. `提供测试数据.xlsx`
   - 当前业务数据与 Excel 计算逻辑的唯一权威来源。
   - 文件受密码保护；密码只从用户或运行环境获取，不写入代码、文档或版本库。
   - 不得用 CSV 覆盖、重建或替代此文件中的公式。
2. `CRM业务数据分析与结算流程优化.md`
   - 财务业务背景、流程目标、已知难点和待确认事项的来源。
3. `csv_output/*.csv`
   - 仅用于数据分析、统计、对账和系统原型输入。
   - CSV 不保存公式、格式和保护设置，不得作为 Excel 业务逻辑的回写源。

如资料之间冲突，以财务确认后的业务口径为准，并记录确认日期、确认人和变更内容。

## 已确认的数据事实

- `1期`：208 条业务数据，61 个字段。
- `2期`：283 条业务数据，61 个字段。
- `服务商信息`：861 条服务商数据，24 个字段。
- `1期` 和 `2期` 的 BH 列标题均为“计量规则”：
  - `1期` 的 BH2:BH209 共 208 个单元格全部为公式。
  - `2期` 的 BH2:BH284 共 283 个单元格全部为公式。
  - 不存在静态值或空白公式单元格。
- BH 列当前计算逻辑依赖 Z 列日期和 BI2 当前日期：
  - Z 列年份等于 2026：`新增量`。
  - Z 列日期距 BI2 少于 13 个月：`新量`。
  - Z 列日期距 BI2 超过 120 个月：`超期`。
  - 其余：`存量`。
- BH 列结果已由业务方确认正确。任何后续处理都必须保留公式及其引用关系，不得只保留计算结果。
- 财务已确认以“设备编号”作为跨期订单唯一标识。2026-08-03 核查 CSV 快照结果：1期 208 条、2期 283 条均无设备编号空值或期内重复；1期 208 个设备编号全部出现在2期，合并491条后有208条跨期重复，按设备编号去重后为283条。

## 脱敏与缺失数据约束

- 财务为避免脱敏后产生歧义，已主动清空部分字段。空值不是可推断值，不得根据相邻字段、简称或历史记录擅自补齐。
- `服务商信息` 中当前完全为空的字段包括：
  - `服务属性`
  - `服务等级`
  - `服务分类`
  - `邮箱`
- 业务表中的 II 服务相关字段、付款周期、变更内容、II 付款方式等填充率较低，可能同时包含“不适用”和“已清空”两种情况；在财务给出口径前不得混为一类。
- 分析中必须区分：
  - 原始空值；
  - 已脱敏但仍有占位符的值；
  - 业务上不适用；
  - 未采集或未知。
- 不输出或尝试还原客户、联系人、手机号、地址、银行账号、统一社会信用代码等敏感信息。

## 已确认要做的工作

### 1. 数据字典与口径确认

- 整理 1期、2期和服务商信息的字段字典。
- 为每个字段记录：业务含义、数据类型、来源、是否脱敏、是否必填、空值含义、统计用途和责任人。
- 明确“新增量、新量、存量、超期”“新装、变更、拆机”“活跃、不活跃”等口径。
- 将尚未确认的规则列为待财务确认项，不把假设写成正式规则。

### 2. 数据质量与一致性检查

- 检查主键/业务键重复、日期顺序、金额异常、状态冲突、服务编号关联失败和必填字段缺失。
- 校验 1期、2期与服务商信息之间的服务编号、供应商、负责人和状态关联。
- 对空值、脱敏值和不适用值分别统计，不以自动填充掩盖数据问题。
- 建立可重复运行的检查清单，并输出异常明细而非直接修改源数据。

### 3. 业务进展统计

- 按月、季度、年度统计新装、变更、拆机。
- 按负责人、供应商、业务类别、业务名称、活跃状态和计量规则拆分。
- 跟踪新增量、新量、存量、超期的数量和金额结构。
- 在目标数据提供后，支持年度目标向月度/季度分解及完成率跟踪。

### 4. 结算测算与结算单候选清单

- 基于活跃订单、上月拆机订单、付费周期、资费、服务费、供应商和计算方式生成结算候选清单。
- 区分月付、年付、两年付、新量、存量及不同服务商政策。
- 保留计算过程、输入字段、规则版本和人工调整说明，确保金额可追溯。
- 在付款、收款和销账数据补充后，扩展到应收、应付、已收、已付、代付和预结算闭环。

### 5. 特殊规则与人工复核

- 当月拆机但次月仍出账的跨期规则需单独建模。
- 首月、拆机月计费需配置专门规则。
- 年付/两年付拆机测算目前与实际账单差异较大，未找到稳定规律前只能：
  - 生成模拟结果；
  - 标记差异；
  - 进入人工复核；
  - 不自动确认结算金额。
- 预结算、补酬金、现金面付和认证过期等场景必须保留人工审批或确认节点。

### 6. 服务商状态与结算资格

- 根据服务状态和计算状态区分正常、暂停、停止及无计算等情况。
- 服务商状态与订单状态冲突时输出异常，不自动选择更有利的状态。
- 付款方式、账户信息、票据类型和税率只用于业务校验，不尝试恢复被清空或脱敏的信息。

### 7. 流程文档与审计

- 整理从 CRM 导出、数据校验、规则计算、人工复核、结算单生成、分发、支付、销账、催欠、补酬金到开票的完整流程。
- 每条规则记录版本、生效日期、确认人、输入、输出、异常处理和验证样例。
- 所有自动化结果应能追溯到源记录和具体规则。

## 暂时不能可靠完成的工作

在缺少对应数据或正式规则时，不承诺以下结果：

- 精确重现电信实际账单，尤其是年付/两年付拆机业务。
- 完整的应收、应付、已收、已付、销账和催欠闭环。
- 自动发送结算单、催欠邮件或补酬金申请。
- 自动完成发票 OCR、平台认证和税务申报。
- 根据已清空或脱敏字段还原客户、联系人、银行账户或服务商明细。
- 使用机器学习解释差异；在获得足量历史实际账单、模拟结果和已确认差异原因前，应优先采用规则与异常检测。

## Excel 与公式维护规则

- 未经明确授权，不修改原始加密工作簿；分析或改造必须先创建副本。
- 不插入、删除或移动 1期/2期的列，除非先完成公式引用影响评估。
- BH 列必须保留为公式列：
  - 不得粘贴为值；
  - 不得由 CSV 回写；
  - 新增业务行时复制相邻公式，并验证行引用；
  - BI2 或 Z 列口径变化时，必须重新核对 BH 全列结果。
- 修改工作簿后至少验证：
  - BH 公式覆盖全部数据行；
  - 公式模式与原逻辑一致；
  - 不出现 `#REF!`、`#VALUE!`、`#NAME?`、`#DIV/0!` 等错误；
  - “新增量/新量/存量/超期”结果抽样与 Excel 原生计算一致。
- 复杂业务规则优先保留为可审计的配置或公式，不把关键判断隐藏在不可见的脚本常量中。

## 输出与安全规范

- CSV 使用 UTF-8 BOM，保留稳定列顺序和表头。
- 分析输出默认只使用脱敏数据；不得在日志、报告或测试样例中扩散敏感值。
- 输出应标明数据来源、统计期间、生成时间、规则版本和已知限制。
- 任何自动发送邮件、更新 CRM 状态、确认付款或生成正式结算单的操作，都需要用户另行明确授权。

## 建议的实施顺序

1. 完成字段字典、缺失值分类和业务键确认。
2. 建立数据质量检查与跨表关联报告。
3. 产出月度/季度新增、拆机、负责人和供应商分析。
4. 把 BH 计量规则整理成正式规则说明和回归测试样例。
5. 生成结算候选清单及年付/两年付人工复核清单。
6. 获取实际账单和收付/销账数据后做差异归因与闭环设计。
7. 规则稳定后，再评估邮件分发、催欠提醒、OCR 和智能异常检测。

## 2026-07-28 已落地成果

- 已生成独立分析工作簿 `outputs/crm_analysis_20260728/CRM业务分析与结算准备工作.xlsx`，未修改原始加密工作簿。
- 分析工作簿包含：
  - `Summary`：数据规模、2期业务概览、近期工作包与重要限制。
  - `业务分析`：1期/2期业务属性、计量规则、负责人和供应商对比。
  - `字段字典`：字段类型、填充率、敏感级别、空值处理与待确认说明。
  - `质量检查`：数据规模、BH完整性、主数据关联和缺失字段检查。
  - `BH规则验证`：491条业务记录的原始BH结果与确认逻辑复算结果。
  - `结算候选`：以2期作为当前较新快照的保守工作分流清单。
  - `方案边界`：现在采用、补数据后实施、后期评估和必须人工介入的能力。
  - 三个原始数据页及来源记录。
- Excel 原生复核结果：
  - 11个工作表、2个分析图表。
  - 491条BH回归记录全部一致，差异数为0。
  - 全工作簿公式错误数为0。
  - 2期283条记录中，44条可进入“初步测算”，239条进入人工复核。
- 当前数据质量待确认项：
  - 2期有1条供应商为空。
  - 2期有12条出账月份为空。
  - 服务编号均可关联到服务商信息。
  - 服务属性、服务等级、服务分类和邮箱仍按财务说明保持为空，不做补齐。
- 结算候选分流是保守的内部工作清单，不代表正式结算资格，不得据此直接生成付款或回写CRM。
- 在财务确认前，2期仅暂作为较新快照用于候选分流；1期和2期不得简单相加为业务总量。

## 2026-08-03 网站交互与导入规则

- `platform/` 已改为无 mock 数据模式：本地快照不可用或尚未导入时，指标和图表显示 `--` 或明确空状态，不加载演示业务记录。
- 真实数值 `0` 与缺失值必须区分：`0` 正常显示为零；空白、无效数字和缺少数据源显示 `--`。
- 浏览器导入支持 CSV、XLS、XLSX、XLSM、XLSB 和 ODS，可一次读取多个文件及每个工作簿的多个 sheet。
- 系统先识别业务表、服务商表和未识别表，再由用户明确选择当前业务工作表；不得把 1期、2期自动相加。
- 用户可明确勾选多个业务工作表进行汇总；多选时必须展示工作表数量和合计行数，并提示确认各 sheet 是否可按同一口径相加。未勾选的业务表不得自动并入。
- 多个业务工作表汇总时必须按设备编号去重，并输出输入行数、保留行数、排除行数、重复设备数和空设备编号数。相同设备优先保留较高期次；无法识别期次时保留完工日期较新的记录；设备编号为空的记录保留并进入质量检查，不得相互合并。
- 月平均计量趋势按 `YYYY-MM` 聚合，日期优先识别完工日期、初始完工日期、最终完工日期、业务完工日期和开通日期，同时兼容 Excel 日期单元格与日期序列值。
- 年份、月份、状态和计量规则使用网格按钮筛选：短按逐项增减以支持多选，“全部”清空该维度限制，长按约 550 毫秒仅保留当前选项。所有筛选必须同步作用于指标、图表和明细。
- 加密工作簿不在浏览器中尝试破解；提示用户另存为未加密副本或 CSV。密码不得保存到前端代码、配置或快照。
- 导入仅在浏览器本地解析，不修改原始 Excel，不回写公式；BH 只消费已有计算结果，原始公式仍以权威工作簿为准。
- 侧边栏入口必须对应实际模块：总览、统一查询、业务分析、销售分析、服务商分析、毛利与目标、结算中心、数据中心。
- ECharts 图表允许点击计量规则、负责人或服务商进行下钻；表格提供筛选、分页和 UTF-8 BOM CSV 导出。
- 网站可读性基线：正文使用微软雅黑/苹方/思源黑体等无衬线中文字体；操作性文字原则上不低于11px，表格、筛选按钮和图表标签不低于12px；ECharts 使用 SVG 渲染以保证浏览器缩放后的文字清晰度。
- 毛利、目标、正式结算、收付、销账和发票等缺少输入时只展示缺失清单与 `--`，不得生成模拟金额。
- 发布构建不得包含 `platform/public/data/local-snapshot.json`；该文件仅用于本地分析，可由 `npm run sync:data` 重建。
