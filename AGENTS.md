# CRM 业务数据分析与结算优化

## 项目目标

基于 CRM 导出数据，逐步形成可复核、可追溯的业务进展分析和结算工作流。当前优先完成数据口径、规则、异常场景和人工审批边界的梳理，再推进统计分析、结算测算和流程自动化。

## 当前资料与优先级

1. `提供测试数据.xlsx`
   - 当前业务数据与 Excel 计算逻辑的唯一权威来源。
   - 文件受密码保护；密码只从用户或运行环境获取，不写入代码、文档或版本库。
   - 不得用 CSV 覆盖、重建或替代此文件中的公式。
2. `CRM业务数据分析与结算流程优化.md`
   - 财务业务背景、流程目标、已知难点和待确认事项的来源。
3. `csv_output/*.csv`
   - 仅用于数据分析、统计、对账和系统原型输入。
   - CSV 不保存公式、格式和保护设置，不得作为 Excel 业务逻辑的回写源。

如资料之间冲突，以财务确认后的业务口径为准，并记录确认日期、确认人和变更内容。

## 已确认的数据事实

- `1期`：208 条业务数据，61 个字段。
- `2期`：283 条业务数据，61 个字段。
- `服务商信息`：861 条服务商数据，24 个字段。
- `1期` 和 `2期` 的 BH 列标题均为“计量规则”：
  - `1期` 的 BH2:BH209 共 208 个单元格全部为公式。
  - `2期` 的 BH2:BH284 共 283 个单元格全部为公式。
  - 不存在静态值或空白公式单元格。
- BH 列当前计算逻辑依赖 Z 列日期和 BI2 当前日期：
  - Z 列年份等于 2026：`新增量`。
  - Z 列日期距 BI2 少于 13 个月：`新量`。
  - Z 列日期距 BI2 超过 120 个月：`超期`。
  - 其余：`存量`。
- BH 列结果已由业务方确认正确。任何后续处理都必须保留公式及其引用关系，不得只保留计算结果。
- 财务于 2026-08-21 确认：`业务属性`只标识初始业务（新装）、原业务变更或拆机，不得作为“新增”判断依据，也不得由计量规则反向补写为新装。除“全年业务拆装情况”等明确要求按业务属性统计的报表外，新增、净增、双线新增及图表新增指标均只统计当前`计量规则`为“新增量”的记录。
- 平台动态重算计量规则须对应 BH 公式：以`初始完工日期`为日期输入，初始完工日期年份为 2026 时为“新增量”；否则相对基准日期月差小于 13 为“新量”，大于 120 为“超期”，其余为“存量”。初始完工日期为空时保留 CRM 已有计量规则，不以完工日期推断。
- 财务已确认以“设备编号”作为跨期订单唯一标识。2026-08-03 核查 CSV 快照结果：1期 208 条、2期 283 条均无设备编号空值或期内重复；1期 208 个设备编号全部出现在2期，合并491条后有208条跨期重复，按设备编号去重后为283条。

## 脱敏与缺失数据约束

- 财务为避免脱敏后产生歧义，已主动清空部分字段。空值不是可推断值，不得根据相邻字段、简称或历史记录擅自补齐。
- `服务商信息` 中当前完全为空的字段包括：
  - `服务属性`
  - `服务等级`
  - `服务分类`
  - `邮箱`
- 业务表中的 II 服务相关字段、付款周期、变更内容、II 付款方式等填充率较低，可能同时包含“不适用”和“已清空”两种情况；在财务给出口径前不得混为一类。
- 分析中必须区分：
  - 原始空值；
  - 已脱敏但仍有占位符的值；
  - 业务上不适用；
  - 未采集或未知。
- 不输出或尝试还原客户、联系人、手机号、地址、银行账号、统一社会信用代码等敏感信息。

## 已确认要做的工作

### 1. 数据字典与口径确认

- 整理 1期、2期和服务商信息的字段字典。
- 为每个字段记录：业务含义、数据类型、来源、是否脱敏、是否必填、空值含义、统计用途和责任人。
- 明确“新增量、新量、存量、超期”“新装、变更、拆机”“活跃、不活跃”等口径。
- 将尚未确认的规则列为待财务确认项，不把假设写成正式规则。

### 2. 数据质量与一致性检查

- 检查主键/业务键重复、日期顺序、金额异常、状态冲突、服务编号关联失败和必填字段缺失。
- 校验 1期、2期与服务商信息之间的服务编号、供应商、负责人和状态关联。
- 对空值、脱敏值和不适用值分别统计，不以自动填充掩盖数据问题。
- 建立可重复运行的检查清单，并输出异常明细而非直接修改源数据。

### 3. 业务进展统计

- 按月、季度、年度统计新装、变更、拆机。
- 按负责人、供应商、业务类别、业务名称、活跃状态和计量规则拆分。
- 跟踪新增量、新量、存量、超期的数量和金额结构。
- 在目标数据提供后，支持年度目标向月度/季度分解及完成率跟踪。

### 4. 结算测算与结算单候选清单

- 基于活跃订单、上月拆机订单、付费周期、资费、服务费、供应商和计算方式生成结算候选清单。
- 区分月付、年付、两年付、新量、存量及不同服务商政策。
- 保留计算过程、输入字段、规则版本和人工调整说明，确保金额可追溯。
- 在付款、收款和销账数据补充后，扩展到应收、应付、已收、已付、代付和预结算闭环。

### 5. 特殊规则与人工复核

- 当月拆机但次月仍出账的跨期规则需单独建模。
- 首月、拆机月计费需配置专门规则。
- 年付/两年付拆机测算目前与实际账单差异较大，未找到稳定规律前只能：
  - 生成模拟结果；
  - 标记差异；
  - 进入人工复核；
  - 不自动确认结算金额。
- 预结算、补酬金、现金面付和认证过期等场景必须保留人工审批或确认节点。

### 6. 服务商状态与结算资格

- 根据服务状态和计算状态区分正常、暂停、停止及无计算等情况。
- 服务商状态与订单状态冲突时输出异常，不自动选择更有利的状态。
- 付款方式、账户信息、票据类型和税率只用于业务校验，不尝试恢复被清空或脱敏的信息。

### 7. 流程文档与审计

- 整理从 CRM 导出、数据校验、规则计算、人工复核、结算单生成、分发、支付、销账、催欠、补酬金到开票的完整流程。
- 每条规则记录版本、生效日期、确认人、输入、输出、异常处理和验证样例。
- 所有自动化结果应能追溯到源记录和具体规则。

## 暂时不能可靠完成的工作

在缺少对应数据或正式规则时，不承诺以下结果：

- 精确重现电信实际账单，尤其是年付/两年付拆机业务。
- 完整的应收、应付、已收、已付、销账和催欠闭环。
- 自动发送结算单、催欠邮件或补酬金申请。
- 自动完成发票 OCR、平台认证和税务申报。
- 根据已清空或脱敏字段还原客户、联系人、银行账户或服务商明细。
- 使用机器学习解释差异；在获得足量历史实际账单、模拟结果和已确认差异原因前，应优先采用规则与异常检测。

## Excel 与公式维护规则

- 未经明确授权，不修改原始加密工作簿；分析或改造必须先创建副本。
- 不插入、删除或移动 1期/2期的列，除非先完成公式引用影响评估。
- BH 列必须保留为公式列：
  - 不得粘贴为值；
  - 不得由 CSV 回写；
  - 新增业务行时复制相邻公式，并验证行引用；
  - BI2 或 Z 列口径变化时，必须重新核对 BH 全列结果。
- 修改工作簿后至少验证：
  - BH 公式覆盖全部数据行；
  - 公式模式与原逻辑一致；
  - 不出现 `#REF!`、`#VALUE!`、`#NAME?`、`#DIV/0!` 等错误；
  - “新增量/新量/存量/超期”结果抽样与 Excel 原生计算一致。
- 复杂业务规则优先保留为可审计的配置或公式，不把关键判断隐藏在不可见的脚本常量中。

## 输出与安全规范

- CSV 使用 UTF-8 BOM，保留稳定列顺序和表头。
- 分析输出默认只使用脱敏数据；不得在日志、报告或测试样例中扩散敏感值。
- 输出应标明数据来源、统计期间、生成时间、规则版本和已知限制。
- 任何自动发送邮件、更新 CRM 状态、确认付款或生成正式结算单的操作，都需要用户另行明确授权。

## 建议的实施顺序

1. 完成字段字典、缺失值分类和业务键确认。
2. 建立数据质量检查与跨表关联报告。
3. 产出月度/季度新增、拆机、负责人和供应商分析。
4. 把 BH 计量规则整理成正式规则说明和回归测试样例。
5. 生成结算候选清单及年付/两年付人工复核清单。
6. 获取实际账单和收付/销账数据后做差异归因与闭环设计。
7. 规则稳定后，再评估邮件分发、催欠提醒、OCR 和智能异常检测。

## 2026-07-28 已落地成果

- 已生成独立分析工作簿 `outputs/crm_analysis_20260728/CRM业务分析与结算准备工作.xlsx`，未修改原始加密工作簿。
- 分析工作簿包含：
  - `Summary`：数据规模、2期业务概览、近期工作包与重要限制。
  - `业务分析`：1期/2期业务属性、计量规则、负责人和供应商对比。
  - `字段字典`：字段类型、填充率、敏感级别、空值处理与待确认说明。
  - `质量检查`：数据规模、BH完整性、主数据关联和缺失字段检查。
  - `BH规则验证`：491条业务记录的原始BH结果与确认逻辑复算结果。
  - `结算候选`：以2期作为当前较新快照的保守工作分流清单。
  - `方案边界`：现在采用、补数据后实施、后期评估和必须人工介入的能力。
  - 三个原始数据页及来源记录。
- Excel 原生复核结果：
  - 11个工作表、2个分析图表。
  - 491条BH回归记录全部一致，差异数为0。
  - 全工作簿公式错误数为0。
  - 2期283条记录中，44条可进入“初步测算”，239条进入人工复核。
- 当前数据质量待确认项：
  - 2期有1条供应商为空。
  - 2期有12条出账月份为空。
  - 服务编号均可关联到服务商信息。
  - 服务属性、服务等级、服务分类和邮箱仍按财务说明保持为空，不做补齐。
- 结算候选分流是保守的内部工作清单，不代表正式结算资格，不得据此直接生成付款或回写CRM。
- 在财务确认前，2期仅暂作为较新快照用于候选分流；1期和2期不得简单相加为业务总量。

## 2026-08-03 网站交互与导入规则

- `platform/` 已改为无 mock 数据模式：本地快照不可用或尚未导入时，指标和图表显示 `--` 或明确空状态，不加载演示业务记录。
- 真实数值 `0` 与缺失值必须区分：`0` 正常显示为零；空白、无效数字和缺少数据源显示 `--`。
- 浏览器导入支持 CSV、XLS、XLSX、XLSM、XLSB 和 ODS，可一次读取多个文件及每个工作簿的多个 sheet。
- 系统先识别业务表、服务商表和未识别表，再由用户明确选择当前业务工作表；不得把 1期、2期自动相加。
- 用户可明确勾选多个业务工作表进行汇总；多选时必须展示工作表数量和合计行数，并提示确认各 sheet 是否可按同一口径相加。未勾选的业务表不得自动并入。
- 多个业务工作表汇总时必须按设备编号去重，并输出输入行数、保留行数、排除行数、重复设备数和空设备编号数。相同设备优先保留较高期次；无法识别期次时保留完工日期较新的记录；设备编号为空的记录保留并进入质量检查，不得相互合并。
- 月平均计量趋势按 `YYYY-MM` 聚合，日期优先识别完工日期、初始完工日期、最终完工日期、业务完工日期和开通日期，同时兼容 Excel 日期单元格与日期序列值。
- 年份、月份、状态和计量规则使用网格按钮筛选：短按逐项增减以支持多选，“全部”清空该维度限制，长按约 550 毫秒仅保留当前选项。所有筛选必须同步作用于指标、图表和明细。
- 加密工作簿不在浏览器中尝试破解；提示用户另存为未加密副本或 CSV。密码不得保存到前端代码、配置或快照。
- 导入仅在浏览器本地解析，不修改原始 Excel，不回写公式；BH 只消费已有计算结果，原始公式仍以权威工作簿为准。
- 侧边栏入口必须对应实际模块：总览、统一查询、业务分析、销售分析、服务商分析、毛利与目标、结算中心、数据中心。
- ECharts 图表允许点击计量规则、负责人或服务商进行下钻；表格提供筛选、分页和 UTF-8 BOM CSV 导出。
- 网站可读性基线：正文使用微软雅黑/苹方/思源黑体等无衬线中文字体；操作性文字原则上不低于11px，表格、筛选按钮和图表标签不低于12px；ECharts 使用 SVG 渲染以保证浏览器缩放后的文字清晰度。
- 毛利、目标、正式结算、收付、销账和发票等缺少输入时只展示缺失清单与 `--`，不得生成模拟金额。
- 发布构建不得包含 `platform/public/data/local-snapshot.json`；该文件仅用于本地分析，可由 `npm run sync:data` 重建。

## 跨会话记忆与交接规范

仓库根目录的 `AGENTS.md` 与 `HANDOFF.md` 共同承担工程记忆，但职责不同：

- `AGENTS.md` 只记录长期有效的业务口径、数据约束、架构原则、安全边界和已确认事实。只有规则经业务确认、架构已经落地或约束长期生效时才更新本文件。
- `HANDOFF.md` 记录当前时点的工作快照，包括分支与提交、已完成内容、未提交改动、验证结果、部署状态、阻塞项、风险和下一步。每次重要交付、部署排障或暂停工作前都应更新。
- 新会话开始时先完整阅读 `AGENTS.md`，再阅读 `HANDOFF.md`，随后执行 `git status --short` 和 `git log -1` 核对实际仓库状态。不得只依据可能过期的交接记录修改代码。
- `HANDOFF.md` 采用“更新当前快照”的方式维护，不追加冗长流水账。已经完成且长期有效的结论应提炼到 `AGENTS.md`，过期的临时信息应从 `HANDOFF.md` 移除。
- 交接记录中的日期和时间统一按 Asia/Shanghai；事实、推测和待确认事项必须明确区分，验证结果应附实际执行命令和结果摘要。
- 不得在两个文件中记录密码、口令、令牌、Cookie、私钥、工作簿解密信息、真实客户或联系人信息、银行信息以及完整业务数据。服务器地址如无必要应使用占位符。
- 交接前至少核对工作树状态、当前提交和与本次改动相称的验证结果；未执行的测试必须明确写为“未执行”，不得根据历史结果推断当前改动已通过。
- 当 `HANDOFF.md` 与当前代码、Git 状态或运行结果冲突时，以现场核查为准并立即修正交接文件；业务、安全和数据处理规则仍以 `AGENTS.md` 及财务最新确认口径为准。

## 2026-08-18 平台架构与部署事实

- 平台已具备管理员登录、服务端文件上传、数据版本管理、多版本选择、按设备编号去重整合和扩展筛选能力。
- 总览页已支持可配置图表模板。模板可设置图表类型、维度、系列、指标、聚合方式、排序和 Top N，并支持创建、编辑、复制、固定、排序、归档与恢复；固定模板跟随总览全局筛选和当前数据版本变化。
- 图表模板与审计记录保存在 `CRM_DATA_DIR` 对应的服务端持久化目录中；容器部署必须挂载持久卷，升级或重建容器时不得删除该数据卷。
- 生产镜像由 GitHub Actions 构建并推送到 GHCR，同时可下载离线镜像压缩包。当前离线压缩包只保证包含 `latest` 标签；若 Compose 配置引用提交 SHA 标签，服务器加载镜像后必须先补充同一 SHA 标签，或将部署配置明确切换为 `latest`，并使用 `--pull never` 避免隔离网络环境访问 GHCR。
- 日常离线更新统一使用 `deploy/offline-deploy.sh`：脚本校验 SHA256、加载 `latest`、仅将 `.env` 的 `CRM_IMAGE` 归一为 `latest`，再以 `--pull never --force-recreate` 重建应用并验证健康状态。不得在日常更新中反复手工打提交 SHA 标签或改写其他环境变量；脚本不得删除持久化数据卷。
- Nginx 以 `/crm/` 子路径代理平台时，必须为 `/crm/assets/` 单独配置到后端 `/assets/` 的代理映射，否则静态资源请求会失败。
- 生产登录 Cookie 使用安全传输约束。只有服务器 IP 且未启用 HTTPS 时可能出现登录后返回登录页；正式部署应配置 HTTPS，即使暂时没有域名也应采用受控证书方案并完成浏览器信任配置。
- 数据版本上传以文件总字节数控制，不按业务行数设限。当前单次原始表格总量上限为 100 MB；Vinext multipart 外层和 Nginx `client_max_body_size` 均设为 110 MB，为表单元数据和 JSON 错误响应留出余量。若真实文件接近该上限，应拆分为多个版本后在数据中心去重整合；继续扩大容量前必须评估流式上传、后台解析和容器内存，不能只提高缓冲上限。
- CRM 全量表允许缺少非必需字段。缺少计量规则、月平均计量或属于年付/两年付只标记为“结算待复核”，不得从版本管理、统一查询、导出或其他已有字段筛选中排除；缺失字段本身不生成筛选选项。界面必须把数据可用性与结算复核状态分开说明。
- 2026-08-18 核查 `第一批活跃3442去密码.xls`：业务明细位于 `Worksheet`，表头为第1行、65列、数据3442行；绝大多数表头带前导空格。字段映射必须忽略表头前后空格、内部空白和换行。`Sheet1` 是汇总/透视类工作表，不应作为业务明细发布。旧版本若已因表头未归一化而保存为空字段快照，代码升级后必须重新上传源表生成新版本，不能从空快照反推字段。
- 平台必须识别历史空字段快照：当未映射记录超过已映射记录时，将版本标记为不可用，禁止激活或参与多版本整合，并提示重新上传原始业务明细表。缺少个别非必需字段不属于该情形，仍可正常发布、查询和筛选。
- 历史数据版本可由管理员永久删除，但当前激活版本以及仍被整合版本引用的父版本必须保留。删除操作须经二次确认、服务端校验和审计记录；删除后只能从原始文件重新上传恢复。
- 自定义图表默认最多展示50个分类，超出部分才允许合并为“其他”。图表模板架构版本2会把仍采用旧默认“Top 10 + 合并其他”的版本1模板迁移为 Top 50；其他管理员自定义数量和关闭“合并其他”的模板保持不变。

## 2026-08-19 整合与展示口径

- 多个原始数据版本按设备编号整合时采用字段级覆盖：按版本发布时间从旧到新处理，同一设备编号由较新版本的非空字段覆盖，较新普通空值保留已有值；空设备编号仍全部保留。普通空值不表示“明确清空”，若未来需要清空已有字段，必须设计单独的可审计标记。
- 历史整合版本不可变。字段级整合上线前生成的整合版本不会自动修复，必须重新选择原始上传版本生成新的整合版本。
- 对外术语严格区分：供应商来自源表“供应商”字段；服务商按 I/II 服务编号及简称识别。平台同时提供独立供应商分析和服务商分析，不以供应商名称替代服务编号。
- 月平均计量在页面与图表中统一按万元展示并保留6位小数；内部聚合继续使用源数值，真实0显示为0，缺失值显示为 `--`。CSV 同时保留原值和万元换算值，便于审计。
- 图表各项数值标签始终显示；旧模板读取时迁移为显示标签。默认系统图表包括供应商进单和按 I 服务编号汇总的服务商进单。
- CSV 导出必须包含设备编号以及全部已建模、允许输出的非敏感核心字段；联系电话只允许输出脱敏结果。分析报表中具有聚合意义的表格应在底部显示总计/总览，比率、均值和占比必须按总体重新计算，不得直接相加。
- “全年业务拆装情况”是财务年度口径：按源表“完工日期”分月，限定统计年度；新装与当年拆机分别按业务属性为新装、拆机统计。该表不得使用“统计完工日期（平台）”的初始完工日期优先规则；后者仍用于其他已有的日期兜底分析。

## 2026-08-21 双线折算口径

- 双线拆装比只使用 CRM 源表“月平均计量”字段，不得以“月平均资费”“优惠资费”或“年优惠资费”替代。没有月平均计量的记录不参与计量折算。
- 月平均计量低于 10,000 元不折算；10,000 至 11,999 元计 1 线，此后每满 2,000 元增加 1 线，单条记录最高 20 线。新增和拆机使用同一口径。
- 双线表必须分列展示原始线路数、符合折算记录数、计量折算线路数和考核合计线路数；考核合计为原始线路数加计量折算线路数。

## 2026-08-21 年度新增复核口径

- 业务属性仅用于标识新装、变更、拆机，不得作为年度新增的判断依据；动态“新增量”仍按已确认的计量规则计算。
- “2026 年度新增复核”以源表“初始完工日期”属于 2026 年的全部线路为新增总体，不得以当前活跃状态、业务属性、当前计量规则或全局筛选排除记录。
- 年度新增复核必须在新增总体内分层展示当前活跃、当年新增当年拆机、其他年度拆机、拆机日期缺失、当前非活跃未拆机和活跃状态缺失；已分层合计应等于新增原始线路数，差额进入人工复核。

## 2026-08-21 当年新增当年拆机确认口径

- 拆机年度及拆机日期一律以源表“完工日期”为准；新增及其余计量规则日期一律以源表“初始完工日期”为准，不以平台的日期兜底字段混用。
- “当年新增当年拆机”仅纳入业务属性为拆机、初始完工日期在统计年度且完工日期也在同一统计年度的记录。总览必须提供独立确认清单，展示设备编号、两类源日期、线路数及当前状态。
- 该确认清单和汇总指标基于当前数据版本的全量记录，不受总览全局年份、活跃状态、业务属性或计量规则筛选影响；筛选后的总览图表仍保持原有联动行为。
- 初始完工日期与完工日期不一致是正常的双日期口径表现，不得作为异常或数据质量问题。仅业务属性为拆机的记录必须具有完工日期；完工日期早于初始完工日期仍作为日期顺序问题进入人工复核。

## 2026-08-21 年度净增长口径

- 总览“2026 年度净增长分析”的新增仅取当前计量规则为“新增量”的记录；该规则以初始完工日期计算，当前新增量年度固定为 2026 年。
- 年度拆机仅取业务属性为拆机且源表“完工日期”在 2026 年的记录。不得将全部历史拆机混入 2026 年年度净增长。
- 年度筛选按业务源日期取数：业务属性为拆机的记录以源表“完工日期”判断年份和月份；其他业务以源表“初始完工日期”判断，初始完工日期缺失时才以完工日期兜底。选择单一年份时，年度新增复核、当年新增当年拆机确认和年度拆机汇总均跟随该年份，但三者继续各自使用已确认的源日期口径。
